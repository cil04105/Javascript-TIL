// console.log(getNum(10));
// function getNum(number) {
//   return number;
// }

// console.log(test(15));
// let test = function (a) {
//   return a;
// };

console.log(myName);
let myName;
myName = "hojin";

// apply : call 함수와 유사하지만, 매개변수는 배열로 받는것에 있어 차이가 있다.
// call : 객채를 바인딩 함과 동시에 호출을 한다.
// bind : 바인딩이된 함수를 반환하며, 한번더 호출시 함수를 실행한다.

// 1. 그냥 일반 함수에서 쓰면 window

// 2. 오브젝트 내의 함수(메소드)에서 쓰면 함수를 동작시키는 오브젝트

// 가 출력된다고 배워봤습니다.

//먼저 기명 함수 앞에 어싱크 키워드를 붙이고 함수 내부에서 동기처리 처럼 작동할 비동기 작업들에 어웨이트를 붙여 작성합니다
